use starterm_workflows_types::*;
pub fn workflow() -> Workflow {
    Workflow {name: r####"Rename a Docker volume"####.into(),command: r####"docker volume create --name {{new_volume}} && docker run --rm -it -v {{old_volume}}:/from -v {{new_volume}}:/to alpine ash -c 'cd /from ; cp -av . /to' && docker volume rm {{old_volume}}"####.into(),tags: vec![r####"docker"####.into()].into_iter().collect(),description: Some(r####"Renames a Docker volume by creating a new volume, copying all of its content to from the old to the new volume and then deletes the old volume."####.into()),arguments: vec![Argument {name: r####"old_volume"####.into(),description: Some(r####"The name of the old volume"####.into()),default_value: None,},Argument {name: r####"new_volume"####.into(),description: Some(r####"The name of the new volume"####.into()),default_value: None,}].into_iter().collect(),source_url: Some(r####"https://github.com/moby/moby/issues/31154#issuecomment-360531460"####.into()),author: Some(r####"Hyruu"####.into()),author_url: Some(r####"https://github.com/Hyruu"####.into()),shells: vec![].into_iter().collect(),}
}
