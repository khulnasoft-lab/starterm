use starterm_workflows_types::*;
pub fn workflow() -> Workflow {
    Workflow {name: r####"Generate a self-signed SSL certificate"####.into(),command: r####"openssl req -x509 -newkey rsa:4096 -keyout {{key_file}} -out {{cert_file}} -sha256 -days {{expiration_days}}"####.into(),tags: vec![r####"ssl"####.into(),r####"openssl"####.into()].into_iter().collect(),description: Some(r####"Generates a self-signed SSL certificate using OpenSSL. Beware, self-signed certificates are typically not validated with any third party. It's recommended to use a certificate signed by a certificate authority."####.into()),arguments: vec![Argument {name: r####"key_file"####.into(),description: None,default_value: Some(r####"key.pem"####.into()),},Argument {name: r####"cert_file"####.into(),description: None,default_value: Some(r####"cert.pem"####.into()),},Argument {name: r####"expiration_days"####.into(),description: Some(r####"The number of days until the certification expires"####.into()),default_value: Some(r####""365""####.into()),}].into_iter().collect(),source_url: Some(r####"https://stackoverflow.com/questions/10175812/how-to-generate-a-self-signed-ssl-certificate-using-openssl"####.into()),author: Some(r####"Diego Woitasen"####.into()),author_url: Some(r####"https://stackoverflow.com/users/1276950/diego-woitasen"####.into()),shells: vec![].into_iter().collect(),}
}
