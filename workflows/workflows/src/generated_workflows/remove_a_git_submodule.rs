use starterm_workflows_types::*;
pub fn workflow() -> Workflow {
    Workflow {name: r####"Remove a git submodule"####.into(),command: r####"git rm {{submodule_path}}"####.into(),tags: vec![r####"git"####.into()].into_iter().collect(),description: Some(r####"Removes all traces of a git submodule from a repository. The `.git` dir of the submodule is kept around in the `/modules` folder of the main projects `.git` dir to make it possible to checkout past commits without requiring fetching from another repository."####.into()),arguments: vec![Argument {name: r####"submodule_path"####.into(),description: Some(r####"The path to the submodule that should be removed"####.into()),default_value: None,}].into_iter().collect(),source_url: Some(r####"https://stackoverflow.com/questions/1260748/how-do-i-remove-a-submodule"####.into()),author: Some(r####"John Douthat"####.into()),author_url: Some(r####"https://stackoverflow.com/users/2774/john-douthat"####.into()),shells: vec![].into_iter().collect(),}
}
